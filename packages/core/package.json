{
  "name": "@planmap/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PlanMap engine — pure TypeScript, zero I/O dependencies. Domain model, seam interfaces, and the impact/drift analysis logic.",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts",
    "./model": "./src/model/index.ts",
    "./testing": "./src/testing.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --max-warnings 0"
  },
  "dependencies": {
    "zod": "^3"
  }
}
